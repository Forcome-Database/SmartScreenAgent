# new-api structured-output fixtures

These are synthetic request/response examples for the OpenAI-compatible
`chat/completions` boundary. They contain no credential, endpoint, prompt body,
completion body from a real candidate, or runtime provider capture.
